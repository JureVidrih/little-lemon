import { StyleSheet } from 'react-native';

export default StyleSheet.create({
    container: {
        width: '100%',
        height: 50,
        flexDirection: 'row',
        justifyContent: 'flex-start',
        alignItems: 'center'
    },
    label: {
        marginLeft: 10,
    },
    checkBoxContainer: {
        width: 28,
        height: 28,
        borderStyle: 'solid',
        borderWidth: 1,
        borderRadius: 8
    }
});