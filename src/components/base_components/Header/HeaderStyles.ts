import { StyleSheet } from 'react-native';

export default StyleSheet.create({
    container: {
        width: '100%',
        height: null
    },
    headerText: {
        marginVertical: 10,
        fontWeight: 'bold'
    }
});