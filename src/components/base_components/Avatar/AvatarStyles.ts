import { StyleSheet } from 'react-native';

export default StyleSheet.create({
    container: {
        backgroundColor: 'gray',
        overflow: 'hidden'
    },
    placeholderLabel: {
        color: '#ffffff'
    }
});